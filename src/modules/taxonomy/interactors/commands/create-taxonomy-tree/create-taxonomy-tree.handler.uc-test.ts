import { describe, expect, it } from 'vitest';

describe('CreateTaxonomyTreeHandler', (): void => {
  it('should create a taxonomy tree', async (): Promise<void> => {
    expect(await Promise.resolve(true)).toBe(true);
  });
});
